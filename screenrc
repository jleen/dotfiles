escape T
nethack on
vbell on
defhstatus 'H:n:t'
startup_message off
defscrollback 10000

# Remove some stupid/dangerous key bindings.
bind k
bind ^k
bind .
bind \\
bind ^h
bind h
# Make them better.
bind \\ quit
bind 'K' kill
bind '}' history
bind 'X' lockscreen

# Start at home.
chdir $HOME
