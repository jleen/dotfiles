[user]
	email = jleen@saturnvalley.org
	name = John Leen

[core]
    # TODO: Don't hardcode ~/sv.
	excludesfile = ~/sv/gitignore
    pager = delta
	quotepath = off

[delta]
    syntax-theme = "Spitfire (rainglow)"
    navigate = true

[interactive]
    diffFilter = delta --color-only

[init]
    defaultBranch = main
